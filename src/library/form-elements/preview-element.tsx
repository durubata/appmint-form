import React from 'react';

export const PreviewElement = (props: { path, name }) => {
  const prop: any = {}
  const handleUpdate = (emoji: any) => {
  };

  return (
    <div >
      <img src={prop.value} alt="Image" className='max-h-32 w-full max-w-48' />
    </div>
  );
};
