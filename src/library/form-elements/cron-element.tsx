import React from 'react';

export const CronElement = (props: { path; name }) => {
  const prop: any = {};

  const handleUpdate = () => { };

  return (
    <div>
      CronElement
    </div>
  );
};
