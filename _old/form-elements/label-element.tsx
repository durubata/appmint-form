import React from 'react';

export const LabelElement = (props: { path, name }) => {
  const prop: any = {}

  const handleUpdate = (emoji: any) => {
  };

  return (
    <h2>{props.name}</h2>
  );
};
